chunking:
  default_strategy: semantic
  chunk_size: 1500
  chunk_overlap: 100
  llm_enhanced: false
  overrides:
    code:
      strategy: code
      chunk_size: 2000
    spreadsheet:
      strategy: exact
      chunk_size: 500
